# 后端 API 基础地址（上线用域名时按需配置）
# 不设置：前端请求相对路径 /api，由 nginx 反向代理到后端（在 nginx/default.conf 中配置 proxy_pass）
# 设置：前端直连该地址，如 https://api.example.com（需后端支持 CORS）
# 构建时使用：VITE_API_BASE_URL=https://api.example.com npm run build
# VITE_API_BASE_URL=
